export type ModulePhalaConfig = {
    modules : {
        phala : {
            api : {
                urls : {
                    WS : string,
                    HTTP : string,
                }
            }
        }
    }
}
